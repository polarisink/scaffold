file-storage:
  enabled: true
  type: local
  local:
    base-path: ./tmp/files

swagger:
  enabled: true

security:
  token:
    jwt:
      secret: ${SCAFFOLD_JWT_SECRET:}
  cors:
    enabled: true
    allowed-origin-patterns:
      - "http://localhost:*"
      - "http://127.0.0.1:*"
